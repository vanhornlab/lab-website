# Repository Guidelines for Jules
- This project is a static site built using the **Hugo** framework.
- All page content lives under `/content/`.
- Layout overrides and custom HTML/CSS templates live under `/layouts/` and `/assets/`.
- Ensure all styling changes remain fully responsive (mobile and desktop).
